# CarineStudy

App estático: `index.html`, `style.css`, `app.js`, `data.js`, `sounds.js`. Vercel sem build. Matéria ativa: **Tecnologia de POA I**.

**Instruções completas (deploy, erros de CSS, mapa de arquivos):** `.claude/CLAUDE.md`

Checklist rápido se o estilo não carrega na Vercel: Root Directory correto; nenhum rewrite que mande tudo para `index.html`; na aba Rede, `style.css` deve ser `text/css`, não `text/html`.
